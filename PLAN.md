# PLAN.md — ブラウザゲーム設計書

## ゲーム名
**Asteroid Blaster** — 迫りくる小惑星を撃ち落とすシューティングゲーム

---

## ゲームの目的
プレイヤーは画面下部に固定された宇宙船を操作し、上から降ってくる小惑星を撃ち落としてスコアを稼ぐ。
できるだけ長く生き残り、ハイスコアを目指す。

---

## プレイヤー操作

| 操作 | アクション |
|------|-----------|
| `←` / `→` キー または `A` / `D` | 宇宙船を左右に移動 |
| `スペース` | レーザーを発射 |
| `P` | ポーズ / 再開 |

---

## メインループ

```
毎フレーム (requestAnimationFrame):
  1. 入力状態を読み取る
  2. プレイヤーを移動する
  3. 弾を移動する（上方向）
  4. 小惑星を移動する（下方向）
  5. 衝突判定:
     - 弾 vs 小惑星 → 両方消去、スコア加算
     - 小惑星 vs プレイヤー → ライフ減少
     - 小惑星が画面下端に到達 → ライフ減少
  6. 新しい小惑星をスポーン（タイマー管理）
  7. 描画 (Canvas 2D API)
  8. HUD（スコア・ライフ・レベル）を更新
```

---

## 勝利条件
- 明示的なクリアはなく、エンドレスモード
- レベルアップごとにファンファーレ表示（通過点として記録）
- オンラインランキング相当の「ローカルハイスコア保存」で達成感を提供

---

## 失敗条件
- ライフが 0 になるとゲームオーバー
- ライフは初期値 3、小惑星が着弾またはすり抜けるたびに -1

---

## 難易度の上がり方

| レベル | 変化内容 |
|--------|---------|
| 1 | 小惑星：低速・少数（3秒ごとに1個） |
| 2 | スポーン間隔 2.5秒、速度 +10% |
| 3〜 | スポーン間隔を最小 0.8秒まで短縮、速度を段階的に最大 3倍まで上昇 |
| 5以降 | 大型（2ヒット必要）・小型（高速）の複数種類が出現 |

レベルアップ条件: **スコアが `level × 500` を超えると次のレベルへ**

---

## 画面構成

```
┌─────────────────────────────────┐
│  SCORE: 1200   LEVEL: 3  ❤❤❤   │  ← HUD（上部バー）
│                                 │
│         ☄  ☄       ☄           │
│                                 │
│              ☄                  │
│                                 │
│                                 │
│             🚀                  │  ← プレイヤー（下部固定レーン）
└─────────────────────────────────┘
```

- **Canvas サイズ**: 480×640px（モバイル縦向きを意識）
- **背景**: 星が流れるパーティクル効果（スクロール）
- **ゲームオーバー画面**: スコア・ハイスコア表示、リトライボタン

---

## 必要なアセット

すべてコードで描画（外部画像ファイル不使用）

| 要素 | 描画方法 |
|------|---------|
| 宇宙船 | Canvas の Path2D（三角形 + ノズル） |
| 小惑星 | 不規則な多角形（ランダム頂点） |
| レーザー | 細長い矩形（グロー効果付き） |
| 背景の星 | 小さな白円のパーティクル |
| 爆発 | 拡散する円パーティクル |
| フォント | システムフォント（monospace） |

---

## 技術スタック

| 層 | 選択 | 理由 |
|----|------|------|
| ビルドツール | **Vite** | 設定ゼロで即起動、HMR対応 |
| 言語 | **TypeScript** | 型安全なゲームロジック |
| レンダリング | **Canvas 2D API** | 軽量・依存ゼロ |
| 状態管理 | クラスベース（純粋TS） | フレームワーク不要 |
| スタイル | CSS（最小限） | Canvas 中心のため |
| パッケージ管理 | **npm** | Node 22 環境 |

---

## 実装順序

### Phase 1 — 土台（最初に遊べるバージョン）
1. `npm create vite` でプロジェクト作成（vanilla-ts テンプレート）
2. `Game` クラス: Canvas 初期化、メインループ (`requestAnimationFrame`)
3. `Player` クラス: 描画・左右移動
4. `Bullet` クラス: 発射・移動・画面外削除
5. `Asteroid` クラス: スポーン・移動・ランダム形状
6. 衝突判定（円形バウンディング）
7. スコア・ライフ・レベルの HUD 表示
8. ゲームオーバー画面 + リトライ

### Phase 2 — 演出強化
- 爆発パーティクル
- 背景スクロール星
- レベルアップ演出

### Phase 3 — ゲームプレイ拡張
- 小惑星の複数種類
- ボム（画面一掃）アイテム
- ローカルハイスコア保存（localStorage）
- モバイルタッチ操作対応
